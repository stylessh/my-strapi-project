{
  "name": "monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "turbo start",
    "lint": "turbo lint",
    "clean": "rm -rf apps/*/dist apps/*/.next apps/*/.turbo node_modules .turbo"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  },
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=20.0.0 <=24.x.x",
    "pnpm": ">=8.0.0"
  }
}
